akka {
  actor {
    provider = cluster
    serialization-bindings {
      "grpcService.client.actors.Message" = jackson-cbor
    }
  }
  remote.artery {
    transport = tcp
    canonical {
      hostname = "127.0.0.1"
      port = 2551
    }
  }

  cluster {
    seed-nodes = [
      "akka://AsyncTestingExampleSpec@127.0.0.1:2551",
      "akka://AsyncTestingExampleSpec@127.0.0.1:2552"
      ]
  }
}